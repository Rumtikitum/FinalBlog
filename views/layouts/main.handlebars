<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TechBlogger</title>
    <head>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.3/css/all.css" integrity="sha384-SZXxX4whJ79/gErwcOYf+zWLeJdY/qpuqC4cAa9rOGUstPomtqpuNWT9wdPEn2fk" crossorigin="anonymous">
  </head>

  <body>
    <div class="wrapper">
      <header>
        <h1 id="forecast" class="row justify-content-center p-3 mb-2 bg-info text-white">
          <i class="fas fa-store-alt" style="margin-right: 5px;"></i>  Tech Blogger!
        </h1>

    <nav class="container-1">
        <ul class="navbar">
        {{#if loggedIn}}
          <li>
            <a href="/" class="btn btn-info" style="width: 80px;">home</a>
          </li>
          <li>
            <button id="logout" class="btn btn-info" style="width: 80px;">logout</button>
          </li>
        {{else}}
        <li>
          <a href="/login" class="btn btn-info">login</a>
        </li>
        {{/if}}
        </ul>
      </nav>
      </header>
      <main>
        <div class="container">
          <div class="row">
            <div class="col">
              {{{body}}}
            </div>
          </div>
        </div>

      </main>
    </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}

  </body>

</html>